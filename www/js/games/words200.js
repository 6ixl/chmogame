/* Словесные игры (пакет 200+) */
(function () {
  const K = window.Kit;
  const W = { cat: 'words' };
  const ANT = [['горячий', 'холодный'], ['большой', 'маленький'], ['день', 'ночь'], ['добрый', 'злой'], ['быстрый', 'медленный'], ['высокий', 'низкий'], ['тяжёлый', 'лёгкий'], ['светлый', 'тёмный'], ['старый', 'молодой'], ['богатый', 'бедный'], ['широкий', 'узкий'], ['твёрдый', 'мягкий'], ['громкий', 'тихий'], ['сухой', 'мокрый'], ['начало', 'конец'], ['друг', 'враг'], ['правда', 'ложь'], ['вверх', 'вниз'], ['открыть', 'закрыть'], ['найти', 'потерять'], ['смеяться', 'плакать'], ['прийти', 'уйти'], ['зима', 'лето'], ['радость', 'грусть'], ['сильный', 'слабый'], ['чистый', 'грязный'], ['полный', 'пустой'], ['дорогой', 'дешёвый'], ['острый', 'тупой'], ['длинный', 'короткий'], ['умный', 'глупый'], ['рано', 'поздно'], ['много', 'мало'], ['далеко', 'близко'], ['победа', 'поражение'], ['купить', 'продать'], ['толстый', 'тонкий'], ['сладкий', 'горький'], ['храбрый', 'трусливый'], ['вход', 'выход']];
  K.pairQuiz(Object.assign({}, W, { id: 'antonyms', title: 'Антонимы', icon: '↔️', desc: 'Подбери слово с противоположным смыслом', data: ANT, both: true, big: 32 }));
  const SYN = [['большой', 'огромный'], ['красивый', 'прекрасный'], ['смелый', 'храбрый'], ['грустный', 'печальный'], ['бежать', 'мчаться'], ['говорить', 'сказать'], ['смотреть', 'глядеть'], ['друг', 'товарищ'], ['дом', 'жилище'], ['быстрый', 'скорый'], ['умный', 'сообразительный'], ['врач', 'доктор'], ['дорога', 'путь'], ['работа', 'труд'], ['ошибка', 'промах'], ['страх', 'ужас'], ['победа', 'триумф'], ['тихий', 'бесшумный'], ['холодный', 'ледяной'], ['весёлый', 'радостный'], ['идти', 'шагать'], ['лгать', 'врать'], ['кушать', 'есть'], ['спешить', 'торопиться'], ['бегемот', 'гиппопотам'], ['лошадь', 'конь'], ['жилище', 'обитель'], ['хороший', 'отличный'], ['бросить', 'кинуть'], ['метель', 'вьюга'], ['алфавит', 'азбука'], ['пожар', 'огонь'], ['солдат', 'боец'], ['спор', 'дискуссия']];
  K.pairQuiz(Object.assign({}, W, { id: 'synonyms', title: 'Синонимы', icon: '🟰', desc: 'Найди слово, близкое по смыслу', data: SYN, both: true, big: 32 }));
  const PROV = [['Без труда не выловишь', 'и рыбку из пруда'], ['Тише едешь —', 'дальше будешь'], ['Не имей сто рублей,', 'а имей сто друзей'], ['Семь раз отмерь —', 'один раз отрежь'], ['Делу время —', 'потехе час'], ['Любишь кататься —', 'люби и саночки возить'], ['Цыплят по осени', 'считают'], ['Не откладывай на завтра', 'то, что можно сделать сегодня'], ['Век живи —', 'век учись'], ['Слово не воробей —', 'вылетит, не поймаешь'], ['Под лежачий камень', 'вода не течёт'], ['Друг познаётся', 'в беде'], ['Волков бояться —', 'в лес не ходить'], ['Яблоко от яблони', 'недалеко падает'], ['Повторение —', 'мать учения'], ['Старый друг', 'лучше новых двух'], ['В гостях хорошо,', 'а дома лучше'], ['Без печали', 'и радости не узнаешь'], ['Всему своё', 'время'], ['Готовь сани летом,', 'а телегу зимой'], ['Кто рано встаёт,', 'тому Бог подаёт'], ['Один в поле', 'не воин'], ['Лучше поздно,', 'чем никогда'], ['Не всё коту', 'масленица'], ['Хлеб всему', 'голова'], ['Дарёному коню', 'в зубы не смотрят'], ['Где тонко,', 'там и рвётся'], ['Ученье — свет,', 'а неученье — тьма'], ['Сделал дело —', 'гуляй смело'], ['Мал золотник,', 'да дорог']];
  K.pairQuiz(Object.assign({}, W, { id: 'proverbs', title: 'Пословицы', icon: '📜', desc: 'Закончи пословицу', data: PROV, big: 24, optSize: 16 }));
  const STRESS = ['звонИт', 'тОрты', 'бАнты', 'щавЕль', 'красИвее', 'кухОнный', 'позвонИм', 'катАлог', 'договОр', 'жалюзИ', 'свЁкла', 'тУфля', 'экспЕрт', 'шАрфы', 'грЕнки', 'коклЮш', 'балОванный', 'включИт', 'облегчИть', 'углубИть', 'откУпорить', 'дОнельзя', 'дОсуг', 'закУпорить', 'Иксы', 'крапИва', 'мастерскИ', 'намерЕние', 'некролОг', 'обеспЕчение', 'оптОвый', 'портфЕль', 'столЯр', 'сирОты', 'валовОй', 'зАгнутый', 'кремЕнь', 'ломОта', 'мЕстностей', 'киломЕтр', 'цемЕнт', 'шофЁр', 'Отрочество', 'новорождЁнный', 'квартАл'];
  const VOW = 'аеёиоуыэюя';
  K.quiz(Object.assign({}, W, { id: 'stress', title: 'Ударения', icon: '🗣', desc: 'Выбери правильное ударение (ЕГЭ-слова)', div: 3, gen: api => { const w = K.pick(STRESS); const low = w.toLowerCase(); const variants = []; for (let i = 0; i < low.length; i++) if (VOW.includes(low[i])) variants.push(low.slice(0, i) + low[i].toUpperCase() + low.slice(i + 1)); const ans = variants.find(v => v === w) || w; return { q: 'Как правильно?', opts: K.opts(api, ans, variants, Math.min(4, variants.length)), ans, big: 26, cols: 1, optSize: 24 }; } }));
  K.quiz(Object.assign({}, W, { id: 'syllables', title: 'Слоги', icon: '🔤', desc: 'Сколько слогов в слове (по гласным)', div: 3, gen: api => { const w = K.pick(K.wordsLen(4, 11)); const n = [...w].filter(c => VOW.includes(c)).length; return { q: w, opts: K.opts(api, n, [n + 1, n - 1, n + 2, n - 2].filter(x => x > 0)), ans: n, big: 36 }; } }));
  const AB = 'абвгдеёжзийклмнопрстуфхцчшщъыьэюя';
  K.quiz(Object.assign({}, W, { id: 'alphagap', title: 'Алфавит', icon: '🔡', desc: 'Какая буква пропущена в ряду алфавита', div: 3, gen: api => { const i = api.rand(1, AB.length - 4); const hide = api.rand(1, 2); const row = AB.slice(i - 1, i + 3).split(''); const ans = row[hide].toUpperCase(); row[hide] = '_'; return { q: row.join(' ').toUpperCase(), opts: K.opts(api, ans, [AB[i - 1 + hide + 1], AB[i - 1 + hide - 1], AB[i + hide + 2], AB[api.rand(0, 32)], AB[api.rand(0, 32)]].map(x => (x || 'я').toUpperCase())), ans, big: 44, optSize: 28 }; } }));
  const EMO = [['🍎', 'яблоко'], ['🐱', 'кошка'], ['🐶', 'собака'], ['🏠', 'дом'], ['🚗', 'машина'], ['🌳', 'дерево'], ['☀️', 'солнце'], ['🌙', 'луна'], ['⭐', 'звезда'], ['🔑', 'ключ'], ['📚', 'книги'], ['⚽', 'мяч'], ['🎁', 'подарок'], ['🍌', 'банан'], ['🐟', 'рыба'], ['🦋', 'бабочка'], ['🌹', 'роза'], ['🍄', 'гриб'], ['✈️', 'самолёт'], ['🚂', 'поезд'], ['⛵', 'парусник'], ['🎸', 'гитара'], ['🥁', 'барабан'], ['🎹', 'пианино'], ['⏰', 'будильник'], ['💡', 'лампочка'], ['🔨', 'молоток'], ['✂️', 'ножницы'], ['👓', 'очки'], ['👑', 'корона'], ['🧀', 'сыр'], ['🥕', 'морковь'], ['🍉', 'арбуз'], ['🐘', 'слон'], ['🦒', 'жираф'], ['🐧', 'пингвин'], ['🐢', 'черепаха'], ['🦔', 'ёж'], ['🌵', 'кактус'], ['☂️', 'зонт'], ['🕯', 'свеча'], ['🧲', 'магнит'], ['🎈', 'шарик'], ['⚓', 'якорь'], ['🪁', 'воздушный змей']];
  K.quiz(Object.assign({}, W, { id: 'emojiname', title: 'Что на картинке', icon: '🖼', desc: 'Назови предмет на картинке', div: 3, gen: api => { const i = api.rand(0, EMO.length - 1); return { q: EMO[i][0], big: 96, opts: K.opts(api, EMO[i][1], EMO.map(e => e[1])), ans: EMO[i][1] }; } }));
  const GROUPS = { 'фрукты': ['яблоко', 'груша', 'слива', 'банан', 'апельсин', 'персик', 'манго', 'киви', 'лимон'], 'животные': ['кошка', 'собака', 'корова', 'лошадь', 'волк', 'лиса', 'заяц', 'медведь', 'олень'], 'мебель': ['стол', 'стул', 'диван', 'шкаф', 'кровать', 'комод', 'кресло', 'полка'], 'транспорт': ['автобус', 'трамвай', 'поезд', 'самолёт', 'велосипед', 'такси', 'метро', 'катер'], 'цвета': ['красный', 'синий', 'зелёный', 'жёлтый', 'белый', 'чёрный', 'серый', 'розовый'], 'профессии': ['врач', 'учитель', 'повар', 'пилот', 'шофёр', 'строитель', 'пекарь', 'юрист'], 'одежда': ['куртка', 'рубашка', 'брюки', 'платье', 'шапка', 'свитер', 'пальто', 'юбка'], 'овощи': ['морковь', 'картофель', 'огурец', 'помидор', 'капуста', 'свёкла', 'редис', 'лук'], 'птицы': ['ворона', 'голубь', 'воробей', 'сова', 'орёл', 'ласточка', 'синица', 'аист'], 'инструменты': ['молоток', 'пила', 'отвёртка', 'дрель', 'топор', 'рубанок', 'клещи', 'напильник'] };
  K.quiz(Object.assign({}, W, { id: 'oddword', title: 'Лишнее слово', icon: '🚫', desc: 'Какое слово не подходит к остальным', div: 3, gen: api => { const keys = Object.keys(GROUPS); const g = K.pick(keys); let o; do { o = K.pick(keys); } while (o === g); const three = api.shuffle(GROUPS[g].slice()).slice(0, 3); const ans = K.pick(GROUPS[o]); return { q: 'Что лишнее?', opts: api.shuffle([...three, ans]), ans, big: 28 }; } }));
  K.quiz(Object.assign({}, W, { id: 'wordlen', title: 'Сколько букв', icon: '🔢', desc: 'Слово мелькнуло — сколько в нём букв?', div: 3, gen: (api, s) => { const w = K.pick(K.wordsLen(4, Math.min(12, 6 + Math.floor(s / 4)))); const n = w.length; const q = api.h('div', { style: 'font-size:38px;font-weight:800;height:60px' }, w); setTimeout(() => { q.textContent = '• • •'; }, Math.max(400, 1300 - s * 40)); return { q, opts: K.opts(api, n, [n + 1, n - 1, n + 2, n - 2]), ans: n }; } }));
  K.quiz(Object.assign({}, W, { id: 'rhymes', title: 'Рифмы', icon: '🎤', desc: 'Выбери слово, которое рифмуется', div: 3, gen: api => { const ws = K.wordsLen(4, 9); for (let t = 0; t < 200; t++) { const w = K.pick(ws); const end = w.slice(-3); const rh = ws.filter(x => x !== w && x.endsWith(end) && !x.endsWith(w) && !w.endsWith(x)); if (!rh.length) continue; const ans = K.pick(rh); const wrong = []; while (wrong.length < 3) { const x = K.pick(ws); if (x.slice(-2) !== w.slice(-2) && !wrong.includes(x)) wrong.push(x); } return { q: w, opts: api.shuffle([ans, ...wrong]), ans, big: 38 }; } return { q: 'кот', opts: ['рот', 'дом', 'лес', 'сад'], ans: 'рот' }; } }));
  K.quiz(Object.assign({}, W, { id: 'novowels', title: 'Без гласных', icon: '🫥', desc: 'Узнай слово, из которого убрали гласные', div: 3, gen: api => { const ws = K.wordsLen(5, 9); const w = K.pick(ws); const sk = s => [...s].filter(c => !VOW.includes(c)).join(''); const same = ws.filter(x => sk(x) === sk(w)); if (same.length > 1) return { q: sk(w).toUpperCase(), opts: K.opts(api, w, same), ans: w, big: 40 }; const wrong = ws.filter(x => x.length === w.length && x[0] === w[0] && x !== w).slice(0, 30); return { q: sk(w).toUpperCase(), opts: K.opts(api, w, wrong.length >= 3 ? wrong : ws.slice(0, 40)), ans: w, big: 40 }; } }));
  K.quiz(Object.assign({}, W, { id: 'extraletter', title: 'Лишняя буква', icon: '➕', desc: 'В слово вставили лишнюю букву — найди её', div: 3, gen: api => { const w = K.pick(K.wordsLen(4, 8)); const L = 'абвгдежзиклмнопрстуфхшя'; let c, pos, t; do { c = K.pick(L); pos = api.rand(1, w.length - 1); t = w.slice(0, pos) + c + w.slice(pos); } while (w[pos] === c || w[pos - 1] === c); const opts = K.opts(api, c.toUpperCase(), [...new Set(w.toUpperCase())]); return { q: t, opts, ans: c.toUpperCase(), big: 38, optSize: 26 }; } }));

  /* ---------- Лесенка слов ---------- */
  K.levelGame(Object.assign({}, W, { id: 'wordladder', title: 'Лесенка слов', icon: '🪜', desc: 'Меняй по одной букве, чтобы превратить одно слово в другое', reward: 10,
    play(c) {
      const { api, h, screen, L } = c; const n = L.lvl < 10 ? 4 : 5;
      const ws = [...new Set(K.wordsLen(n).concat(((window.DICT && DICT.fil) || []).filter(w => w.length === n && /^[а-я]+$/.test(w))))]; const set = new Set(ws);
      const nb = w => { const r = []; for (let i = 0; i < n; i++) for (const ch of 'абвгдежзийклмнопрстуфхцчшщыьэюя') if (ch !== w[i]) { const x = w.slice(0, i) + ch + w.slice(i + 1); if (set.has(x)) r.push(x); } return r; };
      let from, to, path; const want = Math.min(6, 2 + Math.floor(L.lvl / 4));
      for (let t = 0; t < 400; t++) { from = K.pick(ws); const dist = new Map([[from, 0]]); const q = [from]; while (q.length) { const x = q.shift(); if (dist.get(x) >= want) continue; for (const y of nb(x)) if (!dist.has(y)) { dist.set(y, dist.get(x) + 1); q.push(y); } } const far = [...dist].filter(([w, d]) => d === want); if (far.length) { to = K.pick(far)[0]; break; } if (t === 399) { to = K.pick([...dist.keys()].filter(w => w !== from)) || from; } }
      path = [from]; let cur = '';
      const draw = () => { c.top([{ label: 'Шагов', value: path.length - 1 }, { label: 'Минимум', value: want }]); const list = h('div', { style: 'display:flex;flex-direction:column;gap:6px;align-items:center' }, path.map((w, i) => h('div', { class: 'wd-row', style: 'display:flex;gap:4px' }, [...w].map((ch, j) => h('div', { class: 'wd-cell' + (i && path[i - 1][j] !== ch ? ' g' : ''), style: 'width:40px;height:40px;font-size:22px' }, ch.toUpperCase())))));
        const inp = h('div', { style: 'display:flex;gap:4px' }, [...Array(n)].map((_, j) => h('div', { class: 'wd-cell', style: 'width:40px;height:40px;font-size:22px;border:2px dashed var(--accent)' }, (cur[j] || '').toUpperCase())));
        screen.append(h('div', { class: 'game-area', style: 'gap:10px;justify-content:flex-start;overflow:auto' }, h('div', { class: 'hint-text' }, 'Цель: ' + to.toUpperCase() + ' · меняйте одну букву'), list, inp, path.length > 1 ? h('button', { class: 'btn small', onclick: () => { path.pop(); cur = ''; draw(); } }, '↶ Отменить шаг') : null));
        screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(k => { if (k === '⌫') cur = cur.slice(0, -1); else if (k === '⏎') { const last = path[path.length - 1]; const diff = [...cur].filter((ch, j) => ch !== last[j]).length; if (cur.length !== n) api.toast('Нужно ' + n + ' буквы'); else if (diff !== 1) api.toast('Меняйте ровно одну букву'); else if (!K.isWord(cur)) api.toast('Нет такого слова'); else { path.push(cur); api.sound('good'); if (cur === to) { cur = ''; draw(); c.win('Шагов: ' + (path.length - 1)); return; } } cur = ''; } else if (cur.length < n) cur += k; draw(); }))); };
      draw();
    } }));

  /* ---------- Поле чудес ---------- */
  const PC = { 'Животное': ['бегемот', 'крокодил', 'жираф', 'кенгуру', 'носорог', 'барсук', 'енот', 'леопард', 'дельфин', 'хомяк', 'белка', 'лемур', 'бобёр', 'кашалот', 'муравьед'], 'Город России': ['москва', 'казань', 'самара', 'тверь', 'владимир', 'новгород', 'иркутск', 'сочи', 'омск', 'пермь', 'воронеж', 'мурманск', 'калининград', 'ярославль'], 'Профессия': ['космонавт', 'программист', 'пожарный', 'библиотекарь', 'парикмахер', 'водолаз', 'кондитер', 'журналист', 'астроном', 'фермер'], 'Еда': ['пельмени', 'борщ', 'блины', 'окрошка', 'вареники', 'шашлык', 'котлета', 'сырники', 'оливье', 'солянка', 'ватрушка'], 'Музыкальный инструмент': ['балалайка', 'скрипка', 'аккордеон', 'саксофон', 'труба', 'арфа', 'барабан', 'флейта', 'виолончель', 'гармонь'], 'Вид спорта': ['хоккей', 'футбол', 'биатлон', 'теннис', 'волейбол', 'плавание', 'бокс', 'фехтование', 'регби', 'керлинг'], 'Страна': ['бразилия', 'австралия', 'норвегия', 'аргентина', 'индонезия', 'марокко', 'португалия', 'финляндия', 'мексика', 'египет'], 'Растение': ['ромашка', 'подсолнух', 'одуванчик', 'крапива', 'берёза', 'кипарис', 'василёк', 'колокольчик', 'папоротник', 'тюльпан'] };
  Games.register({ id: 'polechudes', title: 'Поле чудес', icon: '🎡', cat: 'words', desc: 'Крути барабан, называй буквы, выигрывай очки', bestLabel: 'Рекорд',
    mount(screen, api) {
      const { h } = api; let word, cat, open, pts, spin, lives, used;
      const SECT = [100, 200, 300, 500, 150, 250, 'x2', 400, 'Б', 350, 600, 50];
      function start() { cat = K.pick(Object.keys(PC)); word = K.pick(PC[cat]); open = new Set(); pts = 0; lives = 4; used = new Set(); spin = null; draw(); }
      function draw() { screen.innerHTML = ''; api.header(screen, [{ label: 'Очки', value: pts }, { label: 'Попыток', value: '❤'.repeat(lives) }, { label: 'Рекорд', value: api.bestOf('polechudes') || 0 }]);
        const board = h('div', { style: 'display:flex;gap:3px;flex-wrap:wrap;justify-content:center' }, [...word].map(ch => h('div', { class: 'wd-cell' + (open.has(ch) || open.has(ch === 'ё' ? 'е' : ch) ? ' g' : ''), style: 'width:30px;height:38px;font-size:20px' }, open.has(ch) || (ch === 'ё' && open.has('е')) ? ch.toUpperCase() : '')));
        const wheel = h('button', { class: 'btn ' + (spin == null ? 'primary' : ''), style: 'font-size:18px;padding:12px 22px', onclick: doSpin }, spin == null ? '🎡 Крутить барабан' : 'Сектор: ' + (spin === 'Б' ? 'Банкрот' : spin === 'x2' ? 'Удвоение' : spin) + ' — назовите букву');
        screen.append(h('div', { class: 'game-area', style: 'gap:18px' }, h('div', { class: 'hint-text', style: 'font-size:16px' }, 'Тема: ' + cat + ' · ' + word.length + ' букв'), board, wheel, h('button', { class: 'btn small', onclick: guessWhole }, 'Назвать слово целиком')));
        const kb = api.keyboard(k => pickLetter(k), { plain: true }); used.forEach(u => kb.mark(u, word.includes(u) ? 'g' : 'b')); screen.append(h('div', { class: 'bottom-bar' }, kb)); }
      function doSpin() { if (spin != null) return; spin = K.pick(SECT); api.sound('select'); if (spin === 'Б') { pts = 0; api.sound('bad'); api.toast('Банкрот! Очки сгорели'); spin = null; } draw(); }
      function pickLetter(k) { if (spin == null) { api.toast('Сначала крутите барабан'); return; } if (used.has(k)) { api.toast('Эта буква уже была'); return; } used.add(k); const n = [...word].filter(ch => ch === k || (k === 'е' && ch === 'ё')).length; if (n) { open.add(k); pts = spin === 'x2' ? Math.max(100, pts * 2) : pts + spin * n; api.sound('good'); } else { lives--; api.sound('bad'); api.vibrate(40); } spin = null; if ([...word].every(ch => open.has(ch) || (ch === 'ё' && open.has('е')))) return finish(true); if (lives <= 0) return finish(false); draw(); }
      function guessWhole() { const inp = h('input', { style: 'width:100%;background:var(--bg2);color:var(--text);border:0;border-radius:10px;padding:10px;font-size:18px' }); api.modal({ title: 'Ваш ответ', body: inp, buttons: [{ label: 'Отмена' }, { label: 'Ответить', cls: 'primary', onClick: () => { if (K.norm(inp.value.trim()) === K.norm(word)) { pts += 500 * [...word].filter(ch => !open.has(ch)).length / 2 | 0; word.split('').forEach(ch => open.add(ch)); finish(true); } else { lives = 0; finish(false); } } }] }); setTimeout(() => inp.focus(), 50); }
      function finish(win) { api.best('polechudes', win ? pts : 0); api.end({ win, title: win ? 'Приз! ' + pts + ' очков' : 'Было слово: ' + word.toUpperCase(), reward: win ? Math.max(5, Math.floor(pts / 100)) : 0, onAgain: start }); }
      start();
    } });

  /* ---------- Слова на букву ---------- */
  Games.register({ id: 'letterwords', title: 'Слова на букву', icon: '🅰️', cat: 'words', desc: 'За 60 секунд вспомни как можно больше слов на заданную букву', bestLabel: 'Рекорд',
    mount(screen, api) {
      const { h } = api; let letter, found, cur, t, timer, hdr, curEl, listEl;
      function start() { letter = K.pick('абвгдкмнопрстл'); found = []; cur = letter; t = 60; draw(); clearInterval(timer); timer = setInterval(() => { t--; hdr.set(1, t); if (t <= 0) { clearInterval(timer); const sc = found.reduce((s, w) => s + w.length - 2, 0); api.best('letterwords', sc); api.end({ title: 'Слов: ' + found.length, reward: Math.floor(sc / 3), text: 'Очки: ' + sc, onAgain: start }); } }, 1000); }
      function draw() { screen.innerHTML = ''; hdr = api.header(screen, [{ label: 'Буква', value: letter.toUpperCase() }, { label: '⏱', value: t }, { label: 'Слов', value: found.length }]); curEl = h('div', { class: 'wow-current', style: 'font-size:30px;min-height:40px' }, cur.toUpperCase()); listEl = h('div', { style: 'display:flex;flex-wrap:wrap;gap:6px;justify-content:center;max-height:30vh;overflow:auto' }, found.map(w => h('span', { class: 'stat' }, w))); screen.append(h('div', { class: 'game-area', style: 'gap:14px' }, curEl, listEl)); screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(key))); }
      function key(k) { if (t <= 0) return; if (k === '⌫') { if (cur.length > 1) cur = cur.slice(0, -1); } else if (k === '⏎') { if (cur.length < 3) api.toast('Минимум 3 буквы'); else if (found.includes(cur)) api.toast('Уже было'); else if (!K.isWord(cur)) { api.toast('Не знаю такого слова'); api.sound('bad'); } else { found.unshift(cur); api.sound('good'); draw(); } cur = letter; } else cur += k; curEl.textContent = cur.toUpperCase(); hdr.set(2, found.length); }
      this.unmount = () => clearInterval(timer); start();
    } });

  /* ---------- Слово-пирамида ---------- */
  K.levelGame(Object.assign({}, W, { id: 'wordpyramid', title: 'Слово-пирамида', icon: '🔺', desc: 'Каждое следующее слово на одну букву длиннее и содержит все буквы предыдущего', reward: 8,
    play(c) {
      const { api, h, screen, L } = c; const goal = Math.min(8, 5 + Math.floor(L.lvl / 5)); let rows = [], cur = '';
      const contains = (big, small) => { const m = {}; for (const ch of big) m[ch] = (m[ch] || 0) + 1; for (const ch of small) { if (!m[ch]) return false; m[ch]--; } return true; };
      const draw = () => { c.top([{ label: 'До', value: goal + ' букв' }]); screen.append(h('div', { class: 'game-area', style: 'gap:6px;justify-content:flex-start;overflow:auto' }, h('div', { class: 'hint-text' }, 'Начните со слова из 3 букв. Потом добавляйте по одной букве (порядок любой)'), ...rows.map(w => h('div', { class: 'stat', style: 'font-size:20px;letter-spacing:.15em' }, w.toUpperCase())), h('div', { class: 'wow-current', style: 'font-size:26px;min-height:36px' }, cur.toUpperCase() || '…'), rows.length ? h('button', { class: 'btn small', onclick: () => { rows.pop(); draw(); } }, '↶ Убрать слово') : null)); screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(k => { if (k === '⌫') cur = cur.slice(0, -1); else if (k === '⏎') { const need = rows.length ? rows[rows.length - 1].length + 1 : 3; if (cur.length !== need) api.toast('Нужно слово из ' + need + ' букв'); else if (rows.length && !contains(cur, rows[rows.length - 1])) api.toast('Должно содержать все буквы прошлого слова'); else if (!K.isWord(cur)) api.toast('Нет такого слова'); else { rows.push(cur); api.sound('good'); if (cur.length >= goal) { cur = ''; draw(); c.win(rows.join(' → ')); return; } } cur = ''; } else cur += k; draw(); }))); };
      draw();
    } }));

  /* ---------- Криптограмма ---------- */
  const PHR = PROV.map(p => p[0] + ' ' + p[1]).concat(['Москва не сразу строилась', 'Утро вечера мудренее', 'Первый блин комом', 'Аппетит приходит во время еды', 'Знание — сила', 'Время — деньги', 'Лучше синица в руках, чем журавль в небе', 'Нет дыма без огня', 'Как аукнется, так и откликнется']);
  K.levelGame(Object.assign({}, W, { id: 'cryptogram', title: 'Криптограмма', icon: '🔐', desc: 'Каждая буква заменена числом. Расшифруй известную фразу', reward: 10,
    play(c) {
      const { api, h, screen, L } = c; const phrase = PHR[(L.lvl * 7) % PHR.length].toLowerCase().replace(/ё/g, 'е'); const letters = [...new Set(phrase.replace(/[^а-яё]/g, ''))]; const code = {}; api.shuffle([...Array(33).keys()].map(i => i + 1)).forEach((n, i) => { if (letters[i]) code[letters[i]] = n; });
      const guess = {}; const show = Math.max(1, Math.floor(letters.length * (0.35 - Math.min(0.25, L.lvl * 0.02)))); api.shuffle(letters.slice()).slice(0, show).forEach(l => guess[code[l]] = l); let sel = null;
      const draw = () => { c.top([{ label: 'Букв', value: letters.length }]); const words = phrase.split(' ').map(w => h('div', { style: 'display:flex;gap:2px;margin:4px 6px' }, [...w].map(ch => { if (!/[а-яё]/.test(ch)) return h('div', { style: 'font-size:20px;align-self:flex-start' }, ch); const n = code[ch]; return h('div', { style: `display:flex;flex-direction:column;align-items:center;width:26px;cursor:pointer;border-radius:6px;background:${sel === n ? 'var(--accent)' : 'var(--card)'}`, onclick: () => { sel = n; api.sound('tap'); draw(); } }, h('b', { style: 'font-size:18px;height:24px' }, (guess[n] || '').toUpperCase()), h('span', { style: 'font-size:10px;color:var(--muted)' }, n)); })));
        screen.append(h('div', { class: 'game-area', style: 'justify-content:flex-start;overflow:auto' }, h('div', { style: 'display:flex;flex-wrap:wrap;justify-content:center;padding-top:10px' }, words), h('div', { class: 'hint-text' }, sel ? 'Выберите букву для числа ' + sel : 'Тапните по клетке с числом'))); screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(k => { if (!sel) return; if (k === '⌫') delete guess[sel]; else if (k === '⏎') return; else { for (const n in guess) if (guess[n] === k) delete guess[n]; guess[sel] = k; } draw(); if (letters.every(l => guess[code[l]] === l)) c.win(phrase); }))); };
      draw();
    } }));

  /* ---------- Буквы по порядку ---------- */
  Games.register({ id: 'tapspell', title: 'Собери слово', icon: '👆', cat: 'words', desc: 'Буквы разбросаны по полю — тапай их по порядку', bestLabel: 'Рекорд',
    mount(screen, api) {
      const { h } = api; let score, t, timer, hdr, word, idx;
      function start() { score = 0; t = 60; next(); clearInterval(timer); timer = setInterval(() => { t--; hdr.set(1, t); if (t <= 0) { clearInterval(timer); api.best('tapspell', score); api.end({ title: 'Время вышло', reward: Math.floor(score / 2), text: 'Слов: ' + score, onAgain: start }); } }, 1000); }
      function next() { word = K.pick(K.wordsLen(4, Math.min(9, 4 + Math.floor(score / 3)))); idx = 0; screen.innerHTML = ''; hdr = api.header(screen, [{ label: 'Слов', value: score }, { label: '⏱', value: t }]); const field = h('div', { style: 'position:relative;width:min(92vw,360px);height:min(60vh,420px);background:var(--bg2);border-radius:16px' }); const target = h('div', { style: 'font-size:28px;font-weight:800;letter-spacing:.2em' }); const paint = () => { target.innerHTML = ''; [...word].forEach((ch, i) => target.append(h('span', { style: 'color:' + (i < idx ? 'var(--green)' : 'var(--text)') }, ch.toUpperCase()))); }; paint(); const pos = []; [...word].forEach((ch, i) => { let x, y; let tries = 0; do { x = api.rand(4, 82); y = api.rand(4, 86); tries++; } while (tries < 50 && pos.some(p => Math.abs(p[0] - x) < 16 && Math.abs(p[1] - y) < 12)); pos.push([x, y]); const el = h('button', { class: 'btn', style: `position:absolute;left:${x}%;top:${y}%;width:48px;height:48px;font-size:22px;padding:0`, onclick: () => { if (word[idx] === ch) { idx++; el.style.visibility = 'hidden'; api.sound('tap'); paint(); if (idx === word.length) { score++; api.sound('good'); if (score % 5 === 0) api.addCoins(3); setTimeout(next, 200); } } else { api.sound('bad'); api.vibrate(30); t = Math.max(1, t - 2); hdr.set(1, t); } } }, ch.toUpperCase()); field.append(el); }); screen.append(h('div', { class: 'game-area', style: 'gap:14px' }, target, field)); }
      this.unmount = () => clearInterval(timer); start();
    } });

  /* ---------- Алфавитный поиск ---------- */
  Games.register({ id: 'alphaguess', title: 'До или после', icon: '🔎', cat: 'words', desc: 'Угадай загаданное слово: подсказка — раньше или позже по алфавиту', bestLabel: 'Меньше попыток',
    mount(screen, api) {
      const { h } = api; let secret, lo, hi, tries, cur, log;
      function start() { secret = K.pick(K.wordsLen(4, 7)); lo = 'а'; hi = 'я'; tries = 0; cur = ''; log = []; draw(); }
      function draw() { screen.innerHTML = ''; api.header(screen, [{ label: 'Попыток', value: tries }, { label: 'Рекорд', value: api.bestOf('alphaguess') || '—' }]); screen.append(h('div', { class: 'game-area', style: 'gap:10px;justify-content:flex-start' }, h('div', { class: 'hint-text' }, 'Слово где-то между'), h('div', { style: 'font-size:22px;font-weight:700' }, lo.toUpperCase() + ' … ' + hi.toUpperCase()), h('div', { class: 'wow-current', style: 'font-size:28px;min-height:36px' }, cur.toUpperCase() || '…'), h('div', { style: 'display:flex;flex-direction:column;gap:4px;overflow:auto;max-height:28vh' }, log.map(l => h('div', { class: 'hint-text' }, l))))); screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(k => { if (k === '⌫') cur = cur.slice(0, -1); else if (k === '⏎') submit(); else cur += k; draw(); }))); }
      function submit() { if (cur.length < 2) return; if (!K.isWord(cur)) { api.toast('Нет такого слова'); return; } tries++; const c = cur.localeCompare(secret, 'ru'); if (cur === secret) { api.best('alphaguess', tries, true); api.end({ title: 'Угадали: ' + secret.toUpperCase(), reward: Math.max(3, 25 - tries), text: 'Попыток: ' + tries, onAgain: start }); return; } if (c < 0) { if (cur.localeCompare(lo, 'ru') > 0) lo = cur; log.unshift(cur.toUpperCase() + ' → загаданное позже'); } else { if (cur.localeCompare(hi, 'ru') < 0) hi = cur; log.unshift(cur.toUpperCase() + ' → загаданное раньше'); } api.sound('select'); cur = ''; }
      start();
    } });

  /* ---------- Словесная змейка ---------- */
  Games.register({ id: 'wordsnake', title: 'Змейка-буквоед', icon: '🐍', cat: 'words', desc: 'Змейка ест буквы по порядку, собирая слово', bestLabel: 'Рекорд',
    mount(screen, api) {
      const N = 14, S = 26, Wd = N * S; let snake, dir, nd, word, idx, letters, score, acc, alive;
      function place() { letters = []; const need = new Set(word.slice(idx)); const pool = [...need, ...K.pick(K.wordsLen(3, 5))].slice(0, 5); for (const ch of pool) { let p; do { p = { x: api.rand(0, N - 1), y: api.rand(0, N - 1) }; } while (snake.some(s => s.x === p.x && s.y === p.y) || letters.some(l => l.x === p.x && l.y === p.y)); letters.push(Object.assign(p, { ch })); } }
      function newWord() { word = K.pick(K.wordsLen(3, Math.min(8, 3 + Math.floor(score / 3)))); idx = 0; place(); }
      function reset() { snake = [{ x: 5, y: 7 }, { x: 4, y: 7 }, { x: 3, y: 7 }]; dir = nd = { x: 1, y: 0 }; score = 0; acc = 0; alive = true; newWord(); a.hdr.set(0, 0); }
      const turn = d => { if (d.x !== -dir.x || d.y !== -dir.y) nd = d; };
      const a = api.arcade(screen, { w: Wd, h: Wd + 40, stats: [{ label: 'Слов', value: 0 }, { label: 'Рекорд', value: api.bestOf('wordsnake') || 0 }], hint: 'Свайп или стрелки. Чужая буква — минус хвост',
        onDown: p => { a._s = p; }, onUp: p => { if (!a._s || p.x < 0) return; const dx = p.x - a._s.x, dy = p.y - a._s.y; a._s = null; if (Math.max(Math.abs(dx), Math.abs(dy)) < 15) return; turn(Math.abs(dx) > Math.abs(dy) ? { x: Math.sign(dx), y: 0 } : { x: 0, y: Math.sign(dy) }); },
        onKey: e => { const m = { ArrowLeft: { x: -1, y: 0 }, ArrowRight: { x: 1, y: 0 }, ArrowUp: { x: 0, y: -1 }, ArrowDown: { x: 0, y: 1 } }[e.key]; if (m) turn(m); },
        frame(dt, ctx) {
          if (alive) { acc += dt; if (acc > Math.max(0.09, 0.2 - score * 0.006)) { acc = 0; dir = nd; const hd = { x: (snake[0].x + dir.x + N) % N, y: (snake[0].y + dir.y + N) % N }; if (snake.some(s => s.x === hd.x && s.y === hd.y)) { alive = false; api.sound('boom'); K.over(api, 'wordsnake', score, 'Укусили себя', 1, reset); } else { snake.unshift(hd); const li = letters.findIndex(l => l.x === hd.x && l.y === hd.y); if (li >= 0) { const l = letters.splice(li, 1)[0]; if (l.ch === word[idx]) { idx++; api.sound('tap'); if (idx === word.length) { score++; a.hdr.set(0, score); api.sound('good'); if (score % 3 === 0) api.addCoins(3); newWord(); } else if (!letters.some(x => x.ch === word[idx])) place(); } else { api.sound('bad'); api.vibrate(30); snake.pop(); if (snake.length > 3) snake.pop(); if (!letters.some(x => x.ch === word[idx])) place(); } } else snake.pop(); } } }
          K.bg(ctx, Wd, Wd + 40, '#12121f'); ctx.fillStyle = '#1b1b30'; for (let i = 0; i < N; i++) for (let j = 0; j < N; j++) if ((i + j) % 2) ctx.fillRect(i * S, 40 + j * S, S, S);
          [...word].forEach((ch, i) => K.text(ctx, ch.toUpperCase(), Wd / 2 - word.length * 11 + i * 22 + 11, 20, 20, i < idx ? '#34d399' : '#fff'));
          letters.forEach(l => { K.rr(ctx, l.x * S + 2, 40 + l.y * S + 2, S - 4, S - 4, 6, '#fbbf24'); K.text(ctx, l.ch.toUpperCase(), l.x * S + S / 2, 40 + l.y * S + S / 2 + 1, 16, '#111'); });
          snake.forEach((s, i) => K.rr(ctx, s.x * S + 1, 40 + s.y * S + 1, S - 2, S - 2, 7, i ? '#22c55e' : '#86efac'));
        } });
      this.unmount = a.stop; reset();
    } });

  /* ---------- Балда ---------- */
  Games.register({ id: 'balda', title: 'Балда', icon: '🅱️', cat: 'words', desc: 'Классика: добавь букву и составь слово на поле 5×5 против бота', bestLabel: 'Побед',
    mount(screen, api) {
      const { h } = api; const N = 5; let g, used, me, bot, turn, lastPass, sel, newCell, path, over, diff = 1, st = K.stats(api, 'balda');
      const dict = [...new Set(K.words().concat(((window.DICT && DICT.fil) || [])).filter(w => /^[а-я]+$/.test(w) && w.length >= 3 && w.length <= 9))];
      const pre = new Set(); dict.forEach(w => { for (let i = 1; i <= w.length; i++) pre.add(w.slice(0, i)); }); const dset = new Set(dict);
      function start() { let w5; const five = dict.filter(w => w.length === 5); w5 = K.pick(five); g = Array(N * N).fill(''); for (let i = 0; i < 5; i++) g[2 * N + i] = w5[i]; used = new Set([w5]); me = 0; bot = 0; turn = 0; sel = -1; newCell = -1; path = []; over = false; draw(); }
      const adj = (a, b) => Math.abs(Math.floor(a / N) - Math.floor(b / N)) + Math.abs(a % N - b % N) === 1;
      const canPlace = i => !g[i] && [i - N, i + N, i % N ? i - 1 : -1, i % N < N - 1 ? i + 1 : -1].some(j => j >= 0 && j < N * N && g[j]);
      function draw(msg) { screen.innerHTML = ''; api.header(screen, [{ label: 'Вы', value: me }, { label: 'Бот', value: bot }, { label: 'П · П', value: st.txt() }]); const size = K.cellSize(screen, N, 330); const grid = K.gridEl(h, N, size); g.forEach((ch, i) => grid.append(h('div', { class: 'pz-cell' + (path.includes(i) ? ' on' : ''), style: `font-size:${size * .5}px;${i === newCell ? 'outline:3px solid var(--gold)' : ''}${canPlace(i) && newCell < 0 ? ';background:var(--card2)' : ''}`, onclick: () => tap(i) }, (ch || '').toUpperCase())));
        const word = path.map(i => g[i]).join('');
        screen.append(h('div', { class: 'game-area', style: 'gap:10px' }, h('div', { class: 'hint-text' }, msg || (newCell < 0 ? '1) Выберите пустую клетку рядом с буквами' : path.length ? 'Слово: ' + word.toUpperCase() : '2) Проведите слово через новую букву (тапами по порядку)')), grid, h('div', { class: 'row' }, h('button', { class: 'btn small', onclick: () => { if (newCell >= 0) g[newCell] = ''; newCell = -1; path = []; draw(); } }, 'Сброс'), h('button', { class: 'btn small primary', onclick: submit }, 'Готово'), h('button', { class: 'btn small', onclick: () => { if (over) return; lastPass = true; api.toast('Вы пропустили ход'); if (newCell >= 0) g[newCell] = ''; newCell = -1; path = []; turn++; setTimeout(botMove, 400); draw(); } }, 'Пропуск'))));
        if (newCell >= 0 && !g[newCell]) screen.append(h('div', { class: 'bottom-bar' }, api.keyboard(k => { g[newCell] = k; draw(); }, { plain: true }))); }
      function tap(i) { if (over) return; if (newCell < 0) { if (canPlace(i)) { newCell = i; draw(); } return; } if (!g[i]) return; if (path.includes(i)) { if (path[path.length - 1] === i) path.pop(); draw(); return; } if (path.length && !adj(path[path.length - 1], i)) return; path.push(i); api.sound('tap'); draw(); }
      function submit() { const w = path.map(i => g[i]).join(''); if (newCell < 0 || !path.includes(newCell)) return draw('Слово должно проходить через новую букву'); if (w.length < 3) return draw('Минимум 3 буквы'); if (used.has(w)) return draw('Это слово уже было'); if (!dset.has(w) && !K.isWord(w)) return draw('Не знаю слова «' + w + '»'); used.add(w); me += w.length; api.sound('good'); newCell = -1; path = []; draw(); if (checkEnd()) return; setTimeout(botMove, 500); }
      function botMove() { if (over) return; let best = null; const empty = g.map((c, i) => i).filter(canPlace); const found = []; for (const cell of empty) for (const ch of 'аеиоуяклмнпрстбвгдзжчшщхцфйыьэюъ') { g[cell] = ch; const dfs = (i, s, seen, has) => { if (!pre.has(s)) return; if (has && s.length >= 3 && dset.has(s) && !used.has(s)) found.push({ cell, ch, s, p: seen.slice() }); for (const j of [i - N, i + N, i % N ? i - 1 : -1, i % N < N - 1 ? i + 1 : -1]) if (j >= 0 && j < N * N && g[j] && !seen.includes(j)) { seen.push(j); dfs(j, s + g[j], seen, has || j === cell); seen.pop(); } }; for (let i = 0; i < N * N; i++) if (g[i]) dfs(i, g[i], [i], i === cell); g[cell] = ''; }
        if (found.length) { found.sort((a, b) => b.s.length - a.s.length); const k = diff === 2 ? 0 : diff === 1 ? Math.floor(Math.random() * Math.min(found.length, 4)) : Math.floor(found.length * (0.4 + Math.random() * 0.6)); best = found[Math.min(found.length - 1, k)]; }
        if (!best) { api.toast('Бот пропускает ход'); draw(); if (lastPass) checkEnd(true); lastPass = false; return; } lastPass = false; g[best.cell] = best.ch; used.add(best.s); bot += best.s.length; path = best.p; newCell = best.cell; api.sound('select'); draw('Бот: ' + best.s.toUpperCase()); setTimeout(() => { path = []; newCell = -1; if (!checkEnd()) draw('Бот составил «' + best.s.toUpperCase() + '». Ваш ход'); }, 1300); }
      function checkEnd(force) { if (g.every(Boolean) || force) { over = true; const win = me > bot; win ? st.win() : st.lose(); api.end({ win, title: win ? 'Победа!' : me === bot ? 'Ничья' : 'Бот победил', reward: win ? 25 : 0, text: me + ' : ' + bot, onAgain: start }); return true; } return false; }
      api.difficulty('balda', d => { diff = d; start(); });
    } });
})();
